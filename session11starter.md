Session 11 prompt — paste this verbatim to start the next session.

You are continuing the IIT Jammu Student Affairs Portal V2. Sessions 1–10 are COMPLETE (the portal is feature‑complete and deployable). You are starting SESSION 11+ — the Member Platform, a large operator‑requested multi‑session program (~6–9 sessions). Run in automode at ultracode; take expert decisions autonomously and record material ones in docs/DECISION_LOG.md (from DL‑058).

STEP 1 — Read before doing anything: docs/SESSION_PROTOCOL.md, CURRENT_STATUS.md, NEXT_TASK.md, TODO.md, KNOWN_ISSUES.md, docs/CHANGELOG.md, and especially docs/MEMBER_PLATFORM_PLAN.md (the durable module‑by‑module design — your primary spec). Then docs/DECISION_LOG.md (esp. DL‑004/006/011 CMS spine; DL‑009/021 cardinality guards; DL‑013/019/020 auth; DL‑026 the additive‑union/no‑deny RBAC rule — M2 revises it; DL‑037/038 the "new module = content_type vs standalone table" rule; DL‑040 audience gating; DL‑046/047/048 dev‑console readers; DL‑049/050/051 admin panel + the ONE mutation route), plus docs/SCHEMA_DESIGN.md and the dev/admin guides.

STEP 2 — Do ONE coherent module per session, end‑to‑end (design → forward migration(s) + seed → service → UI → static + live tests → multi‑agent adversarial review → docs → handoff). Suggested order: M0 → M2 → M1 → M7/M8 spine → M3 → M4 → M5 → M6. Build everything on the existing spine (CMS content types, RBAC, auditedMutation, mapDbError, the dev console) — no parallel pipelines. Unless CURRENT_STATUS.md says a later module is in progress, start with M0.

THE MODULES

M0 — Authentication & account lifecycle (foundation, do first). Email + password ONLY — REMOVE the NextAuth Google provider (keep credentials/argon2id DL‑020, JWT, live per‑request RBAC DL‑019, one account per email). Admin/dev create accounts singly and in BULK (CSV of email → initial password); initial passwords are delivered via the institute's EXTERNAL mail system, NOT the app. Add must_change_password to app_user → force a change on first login (password policy validated client AND server side). Account‑creation request: a public "Request an account" form → centralized notification to admin/dev → admin manually creates the entry + initial password + emails it externally. Forgot/reset: a public form (enter email) → a password‑reset notification in BOTH the admin AND developer Password Management tabs → a stakeholder clicks "Fix" which ASSIGNS it to them (audited) → they use a random‑password generator → set must_change_password → deliver via external mail → user changes it. Admin/dev can force‑reset on suspicion (same flow) and delete users. No self‑serve email‑link reset (admin‑mediated by design).

M1 — User status & access modes. Status active / inactive / revoked (forward‑migrate the enum; reconcile with today's invited/suspended/disabled): active = full; inactive = can log in, browse, see own achievements, cannot participate in events; revoked = cannot log in, sees only the public site. Per‑account "allow normal view on first login" toggle. Three surfaces — public normal view, Admin dashboard, Developer dashboard — plus scoped route RBAC (coordinator → own club page only; secretary → own council; staff → event playground + central announcements) via role_assignment's scope columns.

M2 — RBAC: categories + per‑email overrides. "Categories" = roles (data): seed normal user, coordinator, co‑coordinator, secretary, staff, admin, developer (admin/dev can create more), each with a default role_permission map; categories also power stakeholder grouping/search. Per‑email overrides (NEW): a user_permission_override table (grant | deny) so admin/dev can add OR remove a specific permission from any single email; extend resolveEffectivePermissions (additive role union THEN overrides, deny wins) — this revises DL‑026 #8 (no‑deny), record the new decision, keep the developer/grants_all short‑circuit + the DL‑049 escalation guards. Email‑format smart search: parse <year><level u|p|r><branch><serial>@iitjammu.ac.in (e.g. 2023ume0243@…) + a debounced admin filter (year / level / branch / category / status). Email is the unique identifier everywhere.

M3 — Club/Council pages + memberships. Club detail pages with tabs: Overview (vision/details/PIC/team incl. custom roles an admin/dev defines via position + appointment.title_override), Past events organized, Achievements (the club's Wall‑of‑Fame slice, M4), Resources (exists), Upcoming events (links into the playground, M5), Announcements (club‑specific with opt‑in sync to central), and a Miscellaneous tab of markdown docs that permitted stakeholders add/update/delete. Club membership: a club_membership (app_user ↔ org_unit_lineage, many‑to‑many) — coordinators submit member email lists, admin bulk‑uploads CSV to sync; one user may belong to multiple clubs/societies/chapters; idempotent importer (DL‑031).

M4 — Wall of Fame (achievements). New content_type='achievement' (year‑scoped) with hybrid ordered blocks (markdown / markdown+image / banner / link / gallery) in page_block_payload.data JSONB (DL‑016) or a normalized child table (pick + doc); sanitize markdown (no raw‑HTML injection); reuse media_asset + resolveDeliveryUrl + cloudinaryAutoUrl. Public /wall-of-fame (Server Component) + a per‑club slice. achievement ↔ user/stakeholder mapping so member and club contributions are trackable any time and across an academic year.

M5 — Centralized Event Playground (largest; likely 2 sessions). ONE playground (upgrade /events), login‑only, professional themed UI, hosting any event type (quiz/hackathon/case‑study/…) organized by any stakeholder (club/syndicate/hostel/admin‑defined custom entity). Model = the existing event content_item (DL‑037) + a relational subsystem: event_organizer + event_collaborator (→ org_unit_lineage or a custom‑entity table) with organizer/contributor tags + eligibility criteria; hybrid details + problem statement (markdown + banners + PDFs/sheets/any doc); event_round (stages) · event_registration (per user, partial‑unique dedup, optional capacity → waitlist via a cardinality guard DL‑009/021) · event_score (round + overall) · event_attendance (round‑wise sheets, manually marked); live + past ranking per round AND overall; category/tags (organized‑by) → user/stakeholder performance (M6); CSV downloads (participants/scores/attendance, round + overall) for organizers/admin/staff/developer; a closure window with an OPTIONAL markdown closure report (role + contribution + overall budget) that admin reviews → comments, saving the corrected budget + comment, plus stakeholder feedback (visible to organizers + admin + staff + developer). Plus an "Events Organized" page: a dedicated page listing all organized events with their tagged stakeholders + team members, backed by a curated markdown document that admin / staff / developer add/update (a markdown content type / page_block, year‑scoped) — every add/update is audited (who/what/when, before/after) and that change history is visible, accessible, and downloadable from a named Developer‑dashboard tab (M8). Performance: keyset pagination, indexes on (event, round, user), minimal round‑trips, load handling, low latency.

M6 — Member profiles & performance. A profile page: name, email, syndicate (if any), roles/category, events participated in (category‑mapped), registered/upcoming events (with details), achievements. Self‑view + admin view. Drives per‑stakeholder yearly institute‑contribution tracking.

M7 — Centralized notifications, feedback/issues, announcements. A notification table — centralized, labelled, each with a unique human reference id, entity‑linked, with assignment tracking (who took it) — surfaced on the admin & dev dashboards (password requests, account requests, threshold alerts, …). A standalone feedback/support‑ticket table (DL‑038) — a public feedback/issue/query tab; each carries a unique ref id so a user reports a bug against any component/service id and the developer can trace it; status workflow. Announcements: the central (staff‑managed) board + club‑specific announcements with opt‑in sync‑to‑central; list past / current / upcoming for both announcements AND events. Every notification/feedback/event/announcement carries a unique referenceable id.

M8 — Developer dashboard: audit, analytics, backups, monitoring, mail. Extend audit_log (DL‑012/028) — emphasize cross‑domain actions; a named tab ("Action Log / Change History") makes the full history visible, filterable, and downloadable (CSV/JSON), including every add/update of the M5 "Events Organized" markdown and all other multi‑stakeholder edits. Hidden usage analytics: a page_visit/usage table (most‑visited sections) → dev dashboard, structured + downloadable. Per‑table size monitoring: show each table's size; developer sets a threshold per table; exceeding it flags + posts a dashboard message ("back up this table") while the site + feature keep working past the warning; per‑table backup/export + truncate from the dashboard (reuse the backup_record ledger, Session 8); all exports structured. Resource monitoring: Neon size + Cloudinary usage to plan monthly subscriptions (extend getInfraUsage/reports, DL‑048). Mail (lib/mail/*): nodemailer on the institute VM — bulk + rate‑limited sends with a progress bar ("X of Y sent; N remaining"); senders restricted to an authorized‑sender list maintained by admin/dev (initial passwords still go via external institute mail).

Cross‑cutting (every module): audit every multi‑stakeholder/cross‑domain action (attributed); keep all data synced under the permission model; test each feature in the dev phase; structure data + service boundaries so a future AI trigger/agent layer can read it and automate insights.

GUARD RAILS (unchanged): never prisma db pull / migrate reset — every schema change is a NEW forward migration (Prisma model + raw‑SQL tail for partial uniques/CHECKs/triggers), applied via npm run db:migrate (Prisma CLI reads .env — use db:* scripts). New content_type = a content_type_def seed row + payload table + in‑code handler + the "every type has a handler" test. Authorize FIRST → one semantic audit_log row via auditedMutation → JSON‑safe shapes; honor DB guards via mapDbError. Route mutations through the POST /api/admin/action registry (DL‑050) or a new gated route reusing lib/http/guard.mjs (CSRF + rate limit). Neon has high latency + auto‑suspends (generous live‑test timeouts; re‑run once on a cold‑compute transient). Keep the static suite green and grow it; run the live suite once on a warm Neon; use a multi‑agent workflow for the review/audit pass. Performance is a requirement (the playground especially): keyset pagination, indexes, Server‑Component reads. Professional UI matching the site theme (#003f87, the consolidated next/font fonts).

STEP 3 — Before ending: run the END‑OF‑SESSION checklist (update CURRENT_STATUS, NEXT_TASK, TODO, CHANGELOG, DECISION_LOG [DL‑058+], KNOWN_ISSUES, MILESTONE_PLAN, the docs/MEMBER_PLATFORM_PLAN.md status tracker, DEVELOPER_GUIDE, Token_Usage with workflow tokens + /cost); then output "Done master", one specific commit message, and the handoff naming the next module. Do not commit — the operator commits manually. No Claude icon/attribution in the repo or contributors list.

This is the authoritative prompt (mirrored in NEXT_TASK.md; full design detail per module in docs/MEMBER_PLATFORM_PLAN.md). It begins with M0 — the email+password auth pivot and covers everything you've specified through the Events Organized page.