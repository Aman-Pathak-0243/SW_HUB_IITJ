# Token Usage Log

A permanent, session-by-session record of token usage for the IIT Jammu Portal
V2.0 build. **Updated at the end of every session** (part of the end-of-session
checklist in [SESSION_PROTOCOL.md](SESSION_PROTOCOL.md)).

## How to read this

- **Workflow subagent tokens** are *measured precisely* — each multi-agent
  Workflow run reports its total subagent output tokens. These are recorded
  verbatim.
- **Session total** is the authoritative number from Claude Code's own
  accounting. The orchestration/main-loop usage is **not** separately exposed to
  the assistant mid-session, so where a precise total isn't available it is marked
  *(est.)*. **To get the exact total, run `/cost` in the Claude Code session** and
  paste it into the table.
- Numbers are **output tokens** unless noted; input/context tokens are larger and
  billed separately — `/cost` reflects the true billable total.

## Per-session log

| Session | Date | Focus | Workflow subagent tokens (measured) | Session total | Notes |
|---|---|---|---|---|---|
| 1 | 2026-06-28 | Analysis + Documentation + Architecture | **338,732** (schema-design workflow: 9 agents, 36 tool calls, ~1,017s) | ~0.6M–0.9M *(est.)* — run `/cost` for exact | Also ran 3 Explore subagents in the analysis phase (not separately instrumented). Heavy file reading + ~35 doc writes. No app code. |
| 2 | 2026-06-28 | Database + Prisma + RBAC + Auth | **912,182** (1 adversarial review workflow: 16 agents — 6 reviewers + 10 verifiers, 263 tool calls, ~580s) | _run `/cost` for exact_ | Most work was sequential implementation (schema/migration/seed/auth/RBAC) in the main loop; only the review was a workflow. |
| 3 | 2026-06-28 | CMS Foundation | **978,555** (1 adversarial review workflow: 30 agents — 5 reviewers + 25 verifiers, 266 tool calls, ~548s) | _run `/cost` for exact_ | Implementation (audit extension, content service, handlers, visibility, errors, tests) was sequential in the main loop; the review was the one workflow. 24 confirmed findings fixed. |
| 4 | 2026-06-28 | Academic Year Engine | **866,490** (1 adversarial review workflow: 24 agents — 6 reviewers + 18 verifiers, 191 tool calls, ~418s) | _run `/cost` for exact_ | Implementation (year context/history/transition/lock/public + shared-helper refactor + tests) was sequential in the main loop; the review was the one workflow. 18 confirmed findings → 16 fixed, 2 nits accepted. |
| 5 | 2026-06-28 | Organization Model | **1,235,502** (1 adversarial review workflow: 25 agents — 6 reviewers + 19 verifiers, 389 tool calls, ~744s) | _run `/cost` for exact_ | Implementation (org-unit/people/appointment services, V1 dataset, idempotent importer, public read layer + data-driven pages, the is_singleton forward migration, static + live tests) was sequential in the main loop; the review was the one workflow. 19 findings → 15 confirmed → 13 fixed, 2 accepted. The live DB suite (slow, idempotent importer) was run ~4× during dev. |
| 6 | 2026-06-29 | Events + Announcements | **1,661,214** (1 adversarial review workflow: 64 agents — 8 reviewers + 56 verifiers, 558 tool calls, ~941s) | _run `/cost` for exact_ | Implementation (events/announcements read+import libs, CMS-backed `/api/events`, data-driven pages, base64 handling, audience gating, and the review fixes) was sequential in the main loop; the review was the one workflow. 28 findings → 23 confirmed → 12 fixed, 1 accepted. The live events suite (10 tests) was run 3× during dev (one cold-Neon retry). |
| 7 | 2026-06-29 | Resources + Media | **1,878,894** (the adversarial review, run TWICE: the first run's Verify phase crashed on a `parallel()` thunk bug — 38 agents, **996,415**, findings lost; fixed the script + **resumed** so the 10 Review-phase lens agents returned cached and only the corrected Verify phase ran live — 38 agents, **882,479**. Net unique: 10 lenses + 28 per-finding verifiers, 14 confirmed → all addressed) | _run `/cost` for exact_ | Implementation (media service/cloudinary/migration tool, resources lib+importer+view, config fixes, static + live tests, and the review fixes) was sequential in the main loop; the review was the one workflow (re-run once due to the thunk bug). Live suites: media (3) + resources (4) + events (10) all green; org (4) passed earlier in-session then a **Neon outage** ("Can't reach database server") blocked later re-runs (infra, not logic — failures were on unchanged read paths). |
| 8 | 2026-06-29 | Developer Console | **1,465,840** (1 adversarial review workflow: 43 agents — 7 reviewers + 36 verifiers, 480 tool calls, ~807s) | _run `/cost` for exact_ | Implementation (audit viewer, status/monitoring, reports+cost, backup ledger + recovery delegates, gated routes, CLI, static + live tests, and the review fixes) was sequential in the main loop; the review was the one workflow. 18 findings → 6 confirmed-both + 8 single-vote → all legitimate addressed, 4 rejected. Live suites: dev-console (10) green; org re-confirmed 4/4 (warm Neon, no cold-compute retry needed this session). |
| 9 | 2026-06-29 | Admin Panel | **1,627,717** (1 adversarial review workflow: 45 agents — 7 reviewers + per-finding 2 verifiers, 484 tool calls, ~812s) | _run `/cost` for exact_ | Implementation (the net-new users/roles service, the admin API registry + route, the gated server context/reads, the shell + 6 module UIs + client toolkit, pure helpers, static + live tests, the login guide, and the review fixes incl. a CRITICAL grant-escalation) was sequential in the main loop; the review was the one workflow. 19 findings → 12 confirmed-both + 1 single-vote → all 13 addressed, 6 rejected. The live users/roles suite (6) was run 2× during dev (one to catch the createUser hash-leak, one to confirm the fixes); other live suites untouched (no service changes to them). |
| 10 | 2026-06-29 | Testing + Deployment + Optimization + Handover | **412,946** (1 adversarial review workflow: 13 agents — 5 reviewers + per-finding 2 verifiers, 202 tool calls, ~355s) | _run `/cost` for exact_ | Implementation (CI workflow, CWV/font/brand changes, the CSRF+rate-limit guard, security headers + NFT decision, the V1 prune + Header `/org` cutover, the operator runbook + full docs sweep, and the review fix) was sequential in the main loop; the review was the one workflow. 4 findings → 1 confirmed (CI live-db `if`-scope bug, fixed) + 3 rejected (2 tidied anyway). The full live-DB suite (344 tests) was run once on a warm Neon (~536s, all green, no cold-compute retry). |
| 11 | 2026-06-30 | Member Platform **M0** (auth & account lifecycle) + the PLUGIN | **539,768** (1 adversarial review workflow: 12 agents — 6 dimension finders + per-finding 2 verifiers, 151 tool calls, ~303s) | _run `/cost` for exact_ | Implementation (plugin/feature-flag service, email+password auth pivot + edge middleware, account lifecycle incl. bulk CSV / delete / force-reset / change-own-password, the notification request queue + admin-mediated reset, public + admin UI, 2 forward migrations, static + live tests, and the 3 review fixes) was sequential in the main loop; the review was the one workflow. **3 findings → 3 confirmed-both (0 refuted) → all fixed + re-verified live** (CRITICAL developer-password-reset takeover; notification dedup TOCTOU; Google-reject fail-open). Live suites on warm Neon: m0 (8) + users (6) + smoke (8) green; the other live suites re-confirmed (one Neon-latency transient on the year suite cleared on a warm re-run). |
| 11 | 2026-06-30 | Member Platform **M2** (RBAC categories + per-email overrides + smart search) | **775,159** (1 adversarial review workflow: 12 agents — 6 dimension finders + per-finding 2 verifiers, 150 tool calls, ~376s) | _run `/cost` for exact_ | Implementation (the `user_permission_override` table + migration, the `resolveEffectivePermissions` deny-wins extension, the override service + escalation guard, the 6 seeded category roles + `permission.override`, the pure `lib/users/search.mjs` + the `listUsers` criteria, the debounced filter UI + overrides modal, static + live tests, and the 1 review fix) was sequential in the main loop; the review was the one workflow. **0 confirmed-both + 1 single-vote → the single-vote (a free-text client/server predicate drift) fixed + locked** with a no-drift static test. Live suites on warm Neon: m2 (7) + users (6) green. |
| 11 | 2026-06-30 | Member Platform **M1** (user status & access modes) | **578,360** (1 adversarial review workflow, run TWICE: the first run's Verify phase crashed on a nested-`parallel` script bug — the 6 dimension finders ran, **356,252**, but the per-finding verifier thunks were mis-flattened so two stages' findings were lost; fixed the script + **resumed** so the 6 Find agents returned cached and only the corrected Verify phase ran live — 8 verifiers, **222,108**. Net unique: 6 dimension finders + 4 findings × 2 verifiers) | _run `/cost` for exact_ | Implementation (the enum CREATE-style swap + backfill migration + the pending RBAC index migration, the pure `lib/auth/access.mjs` matrix, the live login/session gates incl. `requireMember`/`assertCanParticipate`/`requireScopedPermission`, the user-service + client-mirror updates, the minimal member view + `loadMemberContext`, the allow-normal-view toggle, static + live tests, and the review fix) was sequential in the main loop; the review was the one workflow (re-run once due to the script bug). **4 raw findings → 1 confirmed-both (an inactive-developer surface-routing dead link — fixed) + 0 single-vote + 3 refuted** (test-coverage nits; 2 of the 3 addressed anyway as cheap coverage adds). Live on warm Neon: m1 (6) green; full live suite run once. |
| 11 | 2026-06-30 | Member Platform **M7 + M8 spine** (notifications/feedback + developer dashboard) | **993,456** (1 adversarial review workflow: 20 agents — 6 dimension finders + per-finding 2 verifiers, 298 tool calls, ~1040s) | _run `/cost` for exact_ | Implementation (the M7/M8 migration + 4 new tables, the notification generalization, the feedback service + public form + admin module, groupByWindow, the audit export, usage analytics + beacon, storage sizes/thresholds/export/truncate, the bulk-mail layer, +5 permissions, the registry actions + routes, 4 new admin/public UIs, static + live tests, and the review fixes) was sequential in the main loop; the review was the one workflow (correct nested-`parallel` this time — no re-run). **7 raw findings → 3 confirmed-both + 2 single-vote → all 5 fixed + 2 refuted:** (medium) exportTable swallowed its ledger write so a PII dump could leave no trail → a GUARANTEED independent audit row; (low) reopening a closed ticket left stale resolvedAt/By → cleared; (high, test) keyset pagination was never walked → a real multi-page no-overlap walk added; (low) mailProgress rounded up to 100% early → floored; (high, test) the batching/pause path was never exercised → ratePerMinute:1 two-batch test. Live on warm Neon: m7 (4) + m8 (3) green (run isolated per #39). |

| 11 | 2026-07-01 | Member Platform **M3** (club/council pages + memberships) | **759,569** (1 adversarial review workflow: 14 agents — 6 dimension finders + per-finding 2 verifiers, 269 tool calls, ~677s) | _run `/cost` for exact_ | Implementation (the `club_membership` M-M + forward migration, the `membership` service+forms+idempotent CSV importer, the pure escape-first markdown renderer, the `club_doc` content type reusing `page_block_payload`, the `announcement_payload.sync_to_central` opt-in + central-or-synced filter, the club reads + `getClubPageView` aggregator, the tabbed `OrgUnitTabs` UI + "my clubs" + the usage beacon, `membership.manage` + registry actions, static + live tests, and the review fixes) was sequential in the main loop; the review was the one workflow. **4 raw findings → 3 confirmed-both + 1 single-vote → all 4 fixed (0 refuted):** (medium) `addMembership` NULLed an existing role on a status-only re-add → role now preserved unless explicitly supplied; (low) `parseMembershipCsv` reported filtered-index line numbers → true file lines; (low) the m3.db teardown leaked the coordinator's `grant_role` audit row → tracked + cleaned; (low, single-vote) no negative-path test on the PII roster read → 401/403 deny assertions added. Static suite **448** green; the **live m3 suite is written but pending the operator's `db:migrate`+`db:seed`** (the build env blocked applying a live migration — KNOWN_ISSUES #42). |

| 11 | 2026-07-01 | Member Platform **M4** (Wall of Fame / student achievements) | **575,103** (1 adversarial review workflow: 8 agents — 6 dimension finders + per-finding 2 verifiers, 190 tool calls, ~430s) | _run `/cost` for exact_ | Implementation (the `achievement` content type + `achievement_payload`/`achievement_credit` forward migration, the pure `lib/achievements/forms.mjs` block/credit validators + the generic-handler `coercePayload` hook, the `credits.mjs` replace-set service, the `public.mjs` batched read layer, `/wall-of-fame` + `AchievementCard` + the club Achievements tab + Header nav, the registry action + audit registry, static + live tests, and the review fix) was sequential in the main loop; the review was the one workflow. **1 raw finding → 1 confirmed-both (0 single-vote) → fixed:** (low) the public achievement shape serialized each credited member's internal `app_user` uuid to anonymous browsers via the client `OrgUnitTabs` (contradicting the display-NAME-only invariant) → `userId` dropped from the public members shape + a live regression assertion added. **Also fixed a latent M3 bug (DL-083)** the first `m3.db` live run surfaced — the generic `writePayload` used `upsert` on a partial edit (Prisma requires the create branch to carry NOT-NULL columns). Live on warm Neon: **m4 (6) + m3 (10) green** (isolated per #39; m4 re-run after the fix); m3.db thereby cleared KNOWN_ISSUES #42. Static suite **466** green. |
| 11 | 2026-07-01 | Member Platform **M5** (Centralized Event Playground) | **1,548,855** (1 adversarial review workflow: 30 agents — 6 dimension finders + per-finding 2 verifiers, 457 tool calls, ~802s = **1,446,406**; + 2 background Agent subagents used during the build: a UI-surface map ~60,891 + the `/admin/events` management-client build ~41,558) | _run `/cost` for exact_ | The largest module. Implementation (the additive `event_payload` columns + the 8-table forward migration with the deferred capacity→waitlist trigger + one-target CHECK; the 10 service modules — authz seam, organizers/entities, rounds, settings, registration+waitlist, scoring+ranking, closure, downloads, organized+change-history, playground read layer; the pure `forms.mjs` + `csv.mjs`; the `coercePayload` event hook; 16 registry actions + the participate/export routes; the login-only playground + detail + organized page + admin module + the M8 change-history tab; static + live tests) was sequential in the main loop; the review + the two build-time Explore/general subagents were the parallel agents. **12 raw findings → 4 confirmed-both + 5 single-vote (3 refuted) → all 9 legitimate fixed:** (medium ×2) organizer cancel/remove didn't auto-promote the waitlist + `promoteEarliestWaitlisted` wasn't concurrency-safe → promotion now runs from every seat-vacating path with `FOR UPDATE SKIP LOCKED`; (medium) the admin round-edit form wiped blank date fields → sends only filled fields; (low) `/events/organized` didn't block revoked/view-disabled → now mirrors `/events`; (PII, ×4 single-vote) the playground/scoring/organizer public shapes leaked the `app_user` uuid → stripped to name-only (DL-082 parity) + a live no-uuid assertion; (single-vote) a re-submitted closure report kept a stale review → cleared. Live on warm Neon: **m5.db 10/10 green** (isolated per #39; re-run after the fixes); static suite **497** green (the suite also caught + fixed a real `trimOrNull` bug). |
| 11 | 2026-07-01 | Member Platform **M6** (Member profiles & performance) | **744,223** (1 adversarial review workflow: 14 agents — 6 dimension finders + per-finding 2 verifiers, 198 tool calls, ~572s) | _run `/cost` for exact_ | The final program module — a READ-ONLY aggregation over the durable M4/M5 ids (NO new table/permission/migration). Implementation (the `lib/member/{profile,contribution,summary}.mjs` read/aggregation layer + `getMemberProfileView` composite, the `listMemberAchievements` addition, the shared `MemberProfile`/`ContributionSummary` Server Components + `/member/profile` + `/admin/users/[id]` + the `/admin/contribution` explorer + the `contribution` nav module, static + live tests) was sequential in the main loop; the review was the one workflow (run concurrently with the live suites). **4 raw findings → 0 confirmed-both + 2 single-vote (2 refuted) → both single-votes fixed + both refuted nits hardened anyway:** (medium, perf) the two profile surfaces re-ran the heaviest read (`listMemberAchievements`) + the current-year lookup twice per render → a ONE `getMemberProfileView` composite hydrates them ONCE and injects into both aggregators; (medium, test) the rank fixture was single-round/single-row so it couldn't distinguish the sum-across-rounds semantic → an overall-score-row fixture added. Live on warm Neon (isolated per #39): **m6.db 8/8 green**; the M5/M4/M3 live suites re-verified (m5 10/10, m4 6/6, m3 10/10); static suite **516** green. |
| 12 | 2026-07-01 | **Consolidation / deploy-hardening** (no new module — full gate, CI single-fork, route-smoke, SOP, member nav, full-site bug audit + fixes) | **2,982,733** (2 background workflows: a per-feature×per-role FULL-SITE AUDIT — 63 agents, 11 area finders + per-finding 2 verifiers, 1026 tool calls, ~1737s = **2,755,005**; + a DIFF REVIEW of the session changes — 6 agents, 6 area finders + verifiers, 110 tool calls, ~199s = **227,728**) | _run `/cost` for exact_ | Implementation (the CI single-fork live job + warm-Neon step, `scripts/route-smoke.mjs` + `test:routes`, `docs/WEBSITE_TESTING_SOP.md`, `MemberNav`/`SignOutButton`, and the **11 bug fixes** B1–B11 incl. the `requireLoggedInAccount` boundary, the shared `lib/csv/cell.mjs` formula-injection guard, the non-destructive membership importer, `promoteWaitlistForCapacity`, fail-closed export auditing, the `/events/[slug]` gate, and the CSV-injection static + m3/m5/m7 live regression assertions) was sequential in the main loop; the two workflows ran in the background. **Audit: 26 raw → 21 confirmed-by-both → 11 fixed + 10 documented-as-accepted** (`docs/CONSOLIDATION_BUGLOG.md`). **Diff review: 0 findings** (no regressions in the fixes). Full live gate re-run PER-FILE single-fork on warm Neon (all Sessions-1–10 + m0–m8 suites); the affected suites (m3/m5/m7 with new assertions + devconsole) green. Static suite **517**; lint + `next build` clean. |
| 13 | 2026-07-01 | **Scoped-coordinator surface** (`/coordinator`, closes KNOWN_ISSUES #43, DL-096) + the **client delivery documentation set** | **1,245,740** (3 background workflows: an UNDERSTAND phase mapping the 6 subsystems for the design — 6 Explore agents, ~245s = **320,832**; the DIFF REVIEW — 5 dimension finders (scope-safety / global-only / RBAC parity / correctness / client-UX), ~334s = **420,447**; the DELIVERY-DOCS authoring — 6 parallel doc agents, ~336s = **504,461**) | _run `/cost` for exact_ | Implementation (the inverse-of-the-resolver `lib/rbac/grants.mjs` scoped-grant discovery + the behaviour-preserving `loadUserRbacInputs` extraction; `lib/events/manage.mjs` `listEventsForManager`/`getManagedEvent`; `lib/coordinator/server.mjs`; the standalone `/coordinator` surface — layout/shell + landing/events/[eventId]/members/contribution pages + `CoordinatorEventClient`/`CoordinatorMembersClient`; the `coordinates` flag + `/member` link; route-smoke; static + live tests) was sequential in the main loop; the three workflows ran in the background. **Diff review: 0 findings** (5 dimensions, 163 tool calls — clean; the diff reuses the proven `assertEventManage`/`resolveEffectivePermissions` seams). No new permission/table/migration/mutation (permissions 52, content types 13). Live on warm Neon (isolated per #39): **coordinator.db 5/5** + regression **m5.db 10/10 + m1.db 6/6** green (the RBAC extraction is behaviour-preserving); static suite **530**; lint + `next build` clean. Delivery docs fact-checked against the repo (counts/commands/routes/pricing-caveats/no-leaked-secrets verified). |

| 14 | 2026-07-02 | **Quick-wins bundle + VM hosting spec** (allowed registrant roles / go-live+countdown / multi-club listing / resource cards / WoF credits UI / bulk overrides; DL-097–102) | **1,366,980** (2 background workflows: an UNDERSTAND sweep mapping all 11 subsystems for the plan — 11 agents, 242 tool calls, ~513s = **882,852**; the DIFF REVIEW — 4 dimension finders (correctness / security-RBAC / data-schema / react-ui) + per-finding verifiers, 121 tool calls, ~270s = **484,128**) | _run `/cost` for exact_ | Implementation (the `event_settings.allowed_registrant_roles` migration + Prisma field; the `registerForEvent` eligibility gate + `getActiveRoleKeys`; the pure `normalizeAllowedRoles`/`isRoleAllowedToRegister`/`registrationPhase`/`formatDuration` + tests; the countdown `RegisterButton` + settings-form role checkboxes + "Go live now"; the multi-club `listClubEvents` union; the resource-card enrichment; the `AchievementCredits` editor; the `setUserOverrides` bulk service + `permission.override.setBulk` + the checkbox-grid `OverridesModal`; `systemRequirements.md`; the doc sweep) was sequential in the main loop; the two workflows ran in the background. **Review: 1 confirmed-both high-severity (of 1 raw) → fixed** — the admin event-settings form submitted blank and would silently clear a coordinator's allowed-roles restriction; fixed by preloading each event's stored settings (matching the coordinator client). One additive migration. Static suite **536**; `eslint` + `next build` clean. **Deferred:** inline edit-on-public-page + live quizzes/SSE+Redis. |

| 15 | 2026-07-02 | **Inline edit-on-public-page** (role + jurisdiction-gated Edit on event / club-profile / wall-of-fame; DL-103) | **293,254** (1 adversarial review workflow: 3 dimension finders (authz-scope / exposure / correctness) + per-finding 2 verifiers, 80 tool calls, ~213s) | _run `/cost` for exact_ | Implementation (the pure `lib/cms/inline.mjs` field specs + `buildEditPatch`; `content.mjs` `editAndPublish` + `resolveInlineEditCapability`; the `content.editAndPublish` action; the client `InlineEditor`; scope threading in `getPlaygroundEvent`/`loadProfile`; wiring the 3 public pages; +6 static tests) was sequential in the main loop; the review was the one workflow. **2 confirmed-both → both fixed:** (HIGH) `editAndPublish` would publish a pre-existing admin WIP draft → now refuses `409 DRAFT_OPEN` + forks from the published revision; (LOW) dead no-op guard → the editor now sends only changed fields. Session-14 migration applied + validated on local Docker Postgres; **no new migration**. Static suite **542**; lint + `next build` clean. **Deferred (last dev item):** live quizzes + SSE/Redis. |

## Running total

- **Measured workflow subagent tokens to date:** 23,781,272 (Session 1: 338,732 + Session 2: 912,182 + Session 3: 978,555 + Session 4: 866,490 + Session 5: 1,235,502 + Session 6: 1,661,214 + Session 7: 1,878,894 + Session 8: 1,465,840 + Session 9: 1,627,717 + Session 10: 412,946 + Session 11 M0: 539,768 + Session 11 M2: 775,159 + Session 11 M1: 578,360 + Session 11 M7/M8: 993,456 + Session 11 M3: 759,569 + Session 11 M4: 575,103 + Session 11 M5: 1,548,855 + Session 11 M6: 744,223 + Session 12 consolidation: 2,982,733 + Session 13 coordinator+docs: 1,245,740 + Session 14 quick-wins: 1,366,980 + Session 15 inline-edit: 293,254)
- **Estimated cumulative session total:** Session 1 ~0.6M–0.9M *(est.)* + Sessions 2–10 *(run `/cost`)*

## Session 1 breakdown (detail)

| Component | Tokens | Source |
|---|---|---|
| Schema-design workflow (3 design + 1 synth + 4 verify + 1 finalize) | 338,732 | Workflow usage report (measured) |
| Analysis-phase Explore subagents (×3) | not instrumented | Agent tool (estimate ~30–50k output) |
| Main orchestration loop (reads + ~35 doc writes + edits) | not separately exposed | use `/cost` |

> **Maintenance note for future sessions:** record each Workflow run's reported
> subagent token count here verbatim, and paste the `/cost` session total when
> closing the session. Keep this table append-only across all 10 sessions.
