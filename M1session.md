You are continuing the IIT Jammu Member Platform (Session 11+ program). The whole program is gated behind the developer-controlled member_platform plugin (/admin/plugins, lib/platform/flags.mjs, DL-058) — off by default; build every new module inside this plugin and gate its surfaces with assertFeatureEnabled/isMemberPlatformEnabled. Done so far: the plugin, M0 (auth & account lifecycle), and M2 (RBAC categories + per-email overrides + smart search). Run in automode at ultracode.

STEP 1 — read: docs/SESSION_PROTOCOL.md, CURRENT_STATUS.md, NEXT_TASK.md, docs/MEMBER_PLATFORM_PLAN.md, and docs/DECISION_LOG.md (esp. DL-058–064 from Session 11, plus DL-019 live per-request RBAC, DL-049 escalation guards, DL-026 #6 the JWT-revocation note, DL-050/051 the one mutation route + gated Server Components).

STEP 2 — do M1 (User status & access modes) end-to-end:
- Forward-migrate the UserStatus enum to active / inactive / revoked (reconcile with today's invited/suspended/disabled — decide & record a DL: e.g. map suspended→inactive, disabled→revoked, keep/retire invited; a forward CREATE-style migration + a data backfill, never an init rewrite, DL-027). Update USER_STATUSES + statusTone + setUserStatus + the self-lockout guard accordingly.
- Enforce the modes LIVE in the session/route layer (DL-019; requireUser + getEffectivePermissions already resolve live): active = full; inactive = can log in + browse + see own achievements but CANNOT participate in events (no event-participation feature exists yet — model the gate as a reusable assertCanParticipate/capability now so M5 reuses it); revoked = cannot log in (reject at the credentials authorize + signIn callback + requireUser), sees only the public site. Add the per-account "allow normal view on first login" toggle.
- Three surfaces + scoped route RBAC: the public normal (member) view, the Admin dashboard, the Developer dashboard, plus scoped route access (coordinator → own club page only; secretary → own council; staff → event playground + central announcements) reusing role_assignment's per-unit/per-year scope columns + resolveEffectivePermissions scope matching (M2). Keep the member view minimal for now (the rich member pages are M3/M6).
- Conventions: authorize-first; one auditedMutation row per status change; JSON-safe shapes; mapDbError; mutations through POST /api/admin/action (registry) or a new gated route reusing lib/http/guard.mjs (CSRF + rate-limit). Pure client-safe helpers mirror server validators (DL-051). Performance: Server-Component reads, minimal round-trips, graceful degradation. Neon has high latency + auto-suspends (generous live-test timeouts).
- Tests: static (status transitions, the access matrix per status, scoped-route gating) + live (m1.db.test.mjs: each status's login/participation/read behavior, scoped grants). Keep the static suite green and grow it (currently 379). Run the live suite once on a warm Neon.
- Then: a multi-agent adversarial review workflow (multi-dimension finders → per-finding 2 verifiers), fix confirmed findings, and run the END-OF-SESSION doc checklist (new DL records; CURRENT_STATUS / NEXT_TASK / TODO / CHANGELOG / KNOWN_ISSUES / SESSION_PROTOCOL / MEMBER_PLATFORM_PLAN / DEVELOPER_GUIDE / Token_Usage). Output "Done master", one commit message, and the next handoff (M7/M8 spine).

Do not commit; no Claude attribution. Operator: after pulling, run `npm run db:migrate` then `npm run db:seed` (both idempotent).

