# Known Issues

Defects, risks, and tech debt found during the Phase 0 analysis. Severity:
🔴 critical · 🟠 high · 🟡 medium · ⚪ low. This file is updated after every
milestone. Details for security items are in [docs/SECURITY.md](docs/SECURITY.md).

| # | Sev | Issue | Location | Status |
|---|-----|-------|----------|--------|
| 1 | 🔴 | **Secrets committed in repo.** A 35-char token and a GitHub PAT (`ghp_…`) are in plaintext in `README.md` (commit `1c88312`). Rotate/remove + purge history. CI gitleaks **temporarily allowlists commits `1c88312` + `6459654` by SHA** so it stays green for new work; **remove that allowlist after rotation + purge**. The literal value was briefly copied into Session-1 docs and has been **redacted**. | `README.md` | In progress — owner owns rotation/removal/purge |
| 2 | 🟠 | **`POST /api/events` has no auth check.** Anyone reachable can create events; UI gate doesn't protect the API. | `app/api/events/route.js` | ✅ Resolved (Session 2) — POST gated by `requirePermission('content.create')`. Full Postgres rebuild still in Session 6. |
| 3 | 🟠 | **`/past-events` was broken.** The V1 client page read `data.success`/`data.events`, but the API returned a bare array → page always empty. | `app/past-events/page.js` | ✅ Resolved (Session 6) — rewritten as a Server Component reading published current-year events from Postgres and splitting past/upcoming via the unit-tested `splitEventsByDate` (no client response-shape assumptions). |
| 4 | 🟠 | **`pdfjs-dist` version mismatch.** Code comments require 3.11.174 (3.x worker path `pdf.worker.min.js`); `package.json` declares `^6.0.227` and imports the `.mjs` worker. Risk: blank PDF previews. | `app/components/PdfSlideshow.jsx`, `package.json` | ✅ Resolved (Session 7, DL-044) — `pdfjs-dist` pinned to exact `6.0.227`; `PdfSlideshow` imports the library AND worker from the same **legacy** build (`pdfjs-dist/legacy/build/pdf.mjs` + `…/pdf.worker.min.mjs`); stale 3.x comment rewritten. `next build` clean. Visual in-browser confirmation is a Session-10 step. |
| 5 | 🟠 | **Base64 images stored in MongoDB.** `/admin` uploaded images as data URLs into `Event.image` → DB bloat, large responses, no validation/size limit. | `app/admin/page.js`, `models/Event.js` | ✅ Resolved (Session 6) — `classifyMedia` turns a `data:` URL into a lightweight `media_asset` PLACEHOLDER (never the blob; DL-039); the CMS-backed `POST /api/events` REJECTS inline base64 (422); the `/admin` form now posts an image URL. The original blob stays in the Session-1 backup; the Session-7 Media tool reconciles the placeholders. |
| 6 | 🟡 | **`process.exit(1)` on DB connect failure.** Kills the web server instead of returning 500. | `lib/db.js` | Open |
| 7 | 🟡 | **Fragile connection caching.** Module-level boolean, not the recommended `global`-cached promise; unreliable in serverless/hot-reload. | `lib/db.js` | Open |
| 8 | 🟡 | **Authorization by hardcoded email allowlist.** Changing admins needs a code change + redeploy; only 2 emails can log in at all. | `app/api/auth/[...nextauth]/route.js` | ✅ Resolved (Session 2) — replaced by DB-driven RBAC (`role`/`permission`/`role_assignment`); admins managed as data. |
| 9 | 🟡 | **No input validation/sanitization** on the events API beyond Mongoose `required`. | `app/api/events/route.js` | ✅ Resolved (Session 6) — the CMS-backed route validates title + audience (enum) app-side (422), rejects base64 images, and surfaces DB-guard violations as friendly mapped errors (`mapDbError`); the CMS service enforces the rest. |
| 10 | ⚪ | **Dead/alternate pages** not routed and partly referencing non-existent routes (`/Clubs/Wellness`, `/student-life`). | `app/page1.js`, `app/admin/page2.js` | ✅ Resolved (Session 10) — `app/admin/page2.js` + V1 `app/admin/page.js` removed in Session 9; **`app/page1.js` removed in Session 10** (unreferenced). |
| 11 | ⚪ | **Inconsistent brand blue.** `#003087` vs `#003f87` used interchangeably. | multiple | ✅ Resolved (Session 10, DL-053) — `#003f87` is the single canonical brand blue everywhere; the stray `#003087` in `EventCard`/`Header`/`admin.css --adm-blue` were aligned. `app/globals.css` documents `--brand-blue`. |
| 12 | ⚪ | **Three font systems coexist** (Geist via next/font; Cormorant+Outfit via `@import`; Georgia inline) → layout shift, duplicate requests. | `app/layout.js`, several components | ✅ Resolved (Session 10, DL-053) — ALL fonts load once via `next/font/google` in `app/layout.js` (Geist, Geist Mono, Cormorant Garamond, Outfit) as CSS variables; the render-blocking per-component `@import` was removed; Header/EventCard/admin.css/Footer consume `var(--font-*)`. |
| 13 | ⚪ | **Massive content duplication.** Four Clubs pages are near-identical copies (~400 lines each). | `app/Clubs/*/page.jsx` | ✅ Resolved (Session 5 logic; **Session 10 cutover**) — one data-driven `<OrgUnitPage>` + `app/org/[type]/[slug]` renders any unit from `lib/org/public.mjs`. Session 10 **removed the four static `app/Clubs/*`** and cut the Header council nav over to `/org/councils/<slug>` (DL-056). |
| 14 | ⚪ | **All pages are Client Components**, including fully static ones → larger bundles, weaker SEO/SSR. | `app/**` | Open — partly mitigated (the data-driven `/org/*`, `/events`, `/past-events`, `/announcements`, and the whole admin panel are Server Components). The remaining static V1 pages (home, Team, Contact-Us, Flagship-events, hostels, messes) stay client; converting them is additive future work, out of scope for the harden-only Session 10. |
| 15 | ⚪ | **No tests, no CI** anywhere in the repo. | repo-wide | ✅ Resolved — 307 static + 59 live tests (Sessions 2–10); **CI added in Session 10** (`.github/workflows/ci.yml`: static suite + lint + build on push/PR; live-DB nightly/secret-gated) alongside the existing `secret-scan.yml`. |
| 16 | ⚪ | **Events cannot be edited or deleted** (create-only API + UI). | `app/api/events`, `app/admin` | ✅ Resolved (Session 6) — events are CMS content, so the full lifecycle (edit draft / publish / unpublish / archive / restore + version history) is available via `lib/cms/content.mjs`. The rich admin UI over these ops is the Session-9 panel. |
| 17 | ⚪ | **Unused image hosts** whitelisted (`images.unsplash.com`, `source.unsplash.com`) but not used. | `next.config.mjs` | ✅ Resolved (Session 7, DL-045) — `remotePatterns` (and `EventsBoard`'s cover allowlist) reduced to `res.cloudinary.com` only; every media asset (V1 + the `/public`→Cloudinary migration output) resolves through it. |
| 18 | ⚪ | **`/public` is ~74 MB** shipped with the app (100 images). | `public/` | Open — operator-pending (DL-056). The **Media Migration Tool** (`npm run db:migrate:media`, DL-043) moves `media_asset` rows to Cloudinary idempotently + reversibly, but (a) it has not been run in this environment and (b) some V1 pages still **hardcode** `/public` paths (e.g. the homepage hero `/hero*.jpg` in `app/page.js`, which are not `media_asset` rows), so a blanket prune would break the live site. Safe prune procedure (verified `--apply` → repoint hardcoded refs → delete only migrated, unreferenced files) is in **OPERATIONS_RUNBOOK.md §3.1**. |
| 19 | 🟠 | **Neon DB credential shared in plaintext chat.** Stored correctly in git-ignored `.env.local`, but if the sharing channel isn't private, rotate the Neon password. Never commit it. | `.env.local` | Open — owner to assess |
| 20 | ⚪ | **Undocumented `queries` collection** (1 doc) found in Mongo `test` db during backup; not referenced in V1 source. | MongoDB `test.queries` | ✅ Resolved (Session 6) — the lone doc is junk test data (`name:"llll"`, `message:"kkk"`) with no V1 consumer → **not migrated** (retained in the verified backup); no `contact_message` module built (it would be a standalone table, not CMS content — DL-038). If a real Contact-Us form is ever added it gets its own table. |
| 21 | ⚪ | **Central audit-write extension.** `audit_log` table + indexes shipped in Session 2; the single Prisma `$extends` write choke point shipped in **Session 3** (`lib/cms/audit.mjs` mounted in `lib/prisma.mjs`) — every CMS mutation now writes one semantic audit row, and the auto extension covers other single-statement mutations (DL-012/DL-025/DL-028). | `lib/prisma.mjs`, `lib/cms/audit.mjs` | ✅ Resolved (Session 3) |
| 22 | ⚪ | **JWT auth-revocation latency.** Authorization + account status are re-checked live per request, so revoked roles / suspended accounts are blocked immediately for protected actions; but a valid token still *authenticates* for ≤24h after sign-out (no server-side token denylist). Acceptable per DL-019. | `lib/auth/options.mjs` | Accepted (DL-019) |
| 23 | ⚪ | **`lock_guard` covers the four spine tables.** It guards org_unit/appointment/content_item (I/U/D) and content_revision (U/D); per-type `*_payload` rows rely on revision immutability rather than their own lock trigger. Add payload-level guards only if direct payload mutation of locked years becomes a real path. | migration raw-SQL | Accepted (documented) |
| 24 | ⚪ | **Audit writes are best-effort, not transactional.** The SEMANTIC audit row is written AFTER the operation's transaction commits, so a crash between COMMIT and the audit insert loses one audit row (no integrity impact). The AUTO extension writes on the un-extended `base` client, so a single-statement write that a future caller wraps in their OWN interactive `$transaction` which then rolls back would leave an orphan audit row. Atomic multi-step flows use the semantic path and never rely on auto-audit; if stronger guarantees are needed, `recordAudit` already accepts a `tx` to move the write inside the transaction (DL-028). | `lib/cms/audit.mjs` | Accepted (DL-028) |
| 26 | ⚪ | **Transition Wizard: non-atomic by design + two accepted edge behaviors.** `runTransition` copies as idempotent statements (not one big transaction) for Neon-latency resilience (DL-031), so an interrupted run leaves the target year *partially* populated until a re-run completes it (safe: re-runs skip done rows and self-heal partial parent wiring; per-content-item clones are atomic). Two accepted edges: (a) a **forced re-sync with a *changed* singleton-position holder** pre-skips the new holder (the prior target appointment stays for manual reconciliation) rather than aborting on `appointment_singleton_position_uq`; (b) `lockBlockReason`'s not-found branch is defensive/test-only (setYearStatus owns the 404), and the appointment/content copy phases derive the source→target unit map two slightly different ways. None affect correctness. | `lib/year/transition.mjs`, `lib/year/lock.mjs` | Accepted (DL-031; adversarial review) |
| 27 | ⚪ | **Full live org import is an operator step, not yet run against 2025-26.** The Session-5 importer (`lib/org/import.mjs`, `npm run db:import:org`) is idempotent + tested end-to-end (live `org.db.test.mjs`), but the full V1 dataset (4 councils / 30 clubs / 6 hostels / 5 messes / committee ≈ 280 audited ops ≈ 15 min on Neon) was not run against the real current year during the session (it's an operator step, like `db:seed`). Re-runs are safe (create 0); a partial run resumes. | `lib/org/import.mjs` | Open — operator to run |
| 28 | ⚪ | **Per-person phone numbers are migrated but not shown on public org pages.** V1 hostel cards displayed warden/attendant phones; the V2 public roster withholds them as PII (DL-034 spirit; the data is stored on `person.phone`). Surface them behind a per-field visibility/consent flag if public display is later wanted. | `lib/org/public.mjs`, `app/components/OrgUnitPage.jsx` | Accepted (PII) |
| 29 | ⚪ | **Events importer resume cannot distinguish an interrupted draft from a deliberately-unpublished one.** The resume step re-publishes a found `status='draft'`, never-archived event so an interrupted run (createDraft committed, publish never ran — DL-031) is completed. A migrated event an editor later **unpublishes** lands in the same structural state (`status='draft'`, `publishedRevisionId=null`, `draftRevisionId` set), so a subsequent `db:import:events` would re-publish it. Archived events ARE protected (the resume requires `status='draft'` AND `archivedAt IS NULL`). Accepted: the importer is a one-shot migration, not a continuous publisher, and re-running it after a manual unpublish is not an expected operational sequence; the same pattern exists in the Session-5 org importer. | `lib/events/import.mjs` | Accepted (DL-037; adversarial review) |
| 25 | ⚪ | **Locked-year append-errata path is not exposed via the CMS service.** `lock_guard` deliberately permits a `content_revision` INSERT in a locked year (append errata), but every service lifecycle op also writes `content_item`, which `lock_guard` blocks — so the supported errata flow is unlock→correct→relock (DL-026), or a raw `content_revision` insert. A dedicated append-revision service function can be added if the append path becomes a real requirement. | `lib/cms/content.mjs` | Accepted (documented) |
| 31 | ⚪ | **Admin Panel: documented scope boundaries (deliberate).** (a) The Users & Roles UI grants roles **institute-wide**; per-unit / per-year SCOPED grants are supported by the service + schema and accepted via the API (`role.grant` accepts `orgUnitLineageKey`/`academicYearId`) but have no dedicated UI control yet. (b) The generic content editor edits a type's SCALAR payload fields (registry-driven); NORMALIZED list children (`club.missionPoints`, `mess.mealTimings`) are still edited via the importer / API, shown read-only in the editor. (c) Content/org/appointment mutations are `requireUser()` + the service's own scope gate (DL-050) — correct for unit-scoped editors, but it means the route does not pre-gate a coarse permission for those ops (the service is the authority; verified every such service authorizes before any state read). | `lib/users/admin.mjs`, `app/admin/*`, `lib/admin/handlers.mjs` | Accepted (DL-049/050/051) |
| 32 | ⚪ | **Turbopack NFT over-tracing warning on `next build`.** The Developer-Console readers (`lib/devconsole/status.mjs` reads the migrations dir; `reports.mjs` reads `docs/Token_Usage.md`) do filesystem ops, so Turbopack conservatively traces the whole project and emits one "Encountered unexpected file in NFT list" note. | `next.config.mjs`, `lib/devconsole/*` | ✅ Decided (Session 10, DL-055) — **accept** the benign warning (build succeeds; routes server-render) AND add `outputFileTracingIncludes` so a serverless build bundles `prisma/migrations/**` + `docs/Token_Usage.md`; the readers also degrade to `{error}` if a file is missing (DL-048). The `turbopackIgnore` hint is not honored for `fs` ops in this Turbopack version (kept as an intent marker). |
| 33 | ⚪ | **No Content-Security-Policy header.** `next.config.mjs#headers()` sets the safe headers (nosniff/X-Frame-Options/Referrer-Policy/Permissions-Policy/HSTS) but NOT a CSP — the public components inject inline `<style>` and the app relies on framework-inlined styles, so a strict CSP needs a nonce pipeline. | `next.config.mjs`, public components | Accepted (Session 10, DL-054) — deferred as the one remaining hardening item; documented in DEPLOYMENT.md. |
| 34 | ⚪ | **Rate limiter is per-process (in-memory).** `lib/http/guard.mjs#makeRateLimiter` bounds `POST /api/admin/action` (60/min/account) + `POST /api/events` (20/min), but each server instance keeps its own window — a coarse abuse dampener, NOT a strict global quota. | `lib/http/guard.mjs` | Accepted (Session 10, DL-054) — front it with a shared store (Upstash/Redis) for a hard global limit; documented in OPERATIONS_RUNBOOK §1.2. |
| 30 | ⚪ | **Developer Console: ungated status building-blocks + `audit.read`-only audit gating (deliberate).** The status sub-helpers (`checkDatabase` / `getMigrationStatus` / `getTransitionStatus` / `getMediaMigrationStatus` / `getInfraUsage`) are individually UNGATED — the gated public surface is the `getSystemStatus` / `getDevConsoleReports` aggregators (`dev.console`) + the routes; this lets the CLI/tests call them directly and keeps the media plan free of a `media.migrate` coupling (it's a pure read reusing `selectMigrationCandidates`). The audit viewer is gated on the dedicated `audit.read` ALONE (narrower than the NEXT_TASK "audit.read / dev.console" wording) because `audit_log` carries PII; list/timeline rows omit ip/user-agent (shown only in the single-entry drill-down). The status payload can also carry partial `{error}` markers per sub-check on a cold Neon (resilience by design). | `lib/devconsole/*` | Accepted (DL-046/047/048; adversarial review) |

| 35 | ⚪ | **Member-platform plugin gating cache (10s, per-process).** `lib/platform/flags.mjs#isFeatureEnabled` caches a flag's state for 10s per process, so a developer's toggle at `/admin/plugins` propagates within ~10s across warm instances (and the edge middleware keys off the JWT must-change flag, not the plugin, so it's inert while off). Fail-closed (a DB error ⇒ off). | `lib/platform/flags.mjs` | Accepted (Session 11, DL-058) |
| 36 | ⚪ | **`deleteUser` hard-deletes; the account's authorship on historical rows is nulled.** A deleted user's `created_by`/`updated_by`/`audit_log.actor` references are `SET NULL` by FK (role_assignments cascade); the deletion event itself is audited with a `before` snapshot, attributed to the deleter. A future "anonymize instead of delete" is additive. Soft states remain via `setUserStatus`. | `lib/users/admin.mjs` | Accepted (Session 11, DL-061) |
| 37 | ⚪ | **Public account routes use the same per-process rate limiter as #34.** `POST /api/account/{request,forgot,password}` wrap `accountRequestLimiter` (10/min, keyed by IP / user) — a coarse abuse dampener, NOT a global quota (front with a shared store for a hard limit, like #34). All wrap `assertSameOrigin` + `assertFeatureEnabled` first. | `lib/http/guard.mjs`, `app/api/account/*` | Accepted (Session 11; DL-054 pattern) |
| 38 | ⚪ | **M2 server-side smart filter is bounded by `take` for a NON-prefixable criterion.** `listUsers` narrows the DB query with an email `startsWith` only when `year[+level[+branch]]` form a *leading* prefix; a non-leading filter (e.g. branch alone) is applied by the precise `matchesUserFilter` over the first `take` (default 200) rows, so a match past row 200 could be missed. The admin UI is unaffected (it filters client-side over the loaded list); only a programmatic caller passing such a filter is bounded. A keyset-paginated user search (M6/M8) removes the bound. | `lib/users/admin.mjs#listUsers` | Accepted (Session 11, DL-064) |
| 39 | ⚪ | **The full live suite runs the DB suites in PARALLEL against one Neon DB → the stateful `year.db` suite can show transient P2025 failures.** vitest runs test files concurrently; the `year.db` suite mutates the SHARED current-year row (`setCurrentYear` / lock / transition), so under concurrent load with the other live suites it can hit a transient "No record was found" (P2025) — observed as 2 failures in a full `RUN_DB_TESTS=1 vitest run` (470/472), while `year.db` passes **6/6 in isolation**. Not a product or M1 defect (M1 doesn't touch the year engine). Run the live suites with a single worker (`--pool=forks --poolOptions.forks.singleFork`) or per-file for a clean full run; CI runs them nightly/secret-gated (DL-052). | `tests/year.db.test.mjs`, vitest config | Accepted (Session 11) — test-harness isolation, not a code defect |
| 40 | ⚪ | **Bulk mail (M8) needs operator config before it can send.** `lib/mail/service.mjs` keeps nodemailer a LAZY + INJECTABLE dependency (DL-073), so the app builds/tests without it; a real `sendBulk` requires `npm install nodemailer` AND `MAIL_HOST`/`MAIL_PORT`/`MAIL_USER`/`MAIL_PASS` on the VM. Until then `sendBulk` returns a friendly `MAIL_NOT_CONFIGURED` / `MAIL_NOT_INSTALLED` 503 and the allowlist + composer UI work but sends fail closed. Initial account passwords still go via the institute's external mail, never here. | `lib/mail/service.mjs` | Operator step (Session 11, DL-073) |
| 41 | ⚪ | **Usage analytics are not yet auto-recorded from the client.** M8 ships the `page_visit` table, the best-effort `recordPageVisit`, the `POST /api/usage` beacon, and the `getUsageAnalytics` dashboard read (DL-071), but no client-side beacon fired on navigation yet. | `app/api/usage`, root layout | ✅ Resolved (Session 11 / M3, DL-079) — a `UsageBeacon` Client Component (`app/components/UsageBeacon.jsx`) is mounted in the root layout and fires a best-effort `POST /api/usage` (`{ path, section }`) per client navigation; inert when the plugin is off (the route returns 204). `page_visit` now fills on real traffic. |
| 42 | ⚪ | **M3 live suite (`m3.db`) is written but was NOT run against Neon in the build environment.** ~~The Session-11/M3 forward migration + seed are the operator's step here~~. | `tests/m3.db.test.mjs`, `prisma/migrations/20260701120000_member_platform_m3` | ✅ Resolved (Session 11 / M4) — the M3 migration was already applied; the M4 session ran `npm run db:seed` (adding the `club_doc` type + `membership.manage` → 51 perms) and ran the M3 live suite on warm Neon: **it surfaced a latent bug** (the generic `writePayload` used `upsert` on a partial edit, which Prisma rejects because the `create` branch must carry NOT-NULL payload columns like `announcement.body` — the sync-to-central test edits `{syncToCentral:true}` alone). Fixed by using `UPDATE` on `isCreate:false` (**DL-083**); `m3.db` now **10/10 green** and `events.db` re-confirmed green (no regression from the central-announcement filter). |
| 43 | ⚪ | **A club-scoped coordinator manages their events via the API, not (yet) via the admin nav.** M5's `assertEventManage` seam lets a coordinator whose `event.manage` is SCOPED to a tagged organizing club lineage run that event (rounds/scores/attendance/settings/registrations/closure) — proven live in `m5.db`. But `loadAdminContext` resolves the admin NAV at GLOBAL scope (like every admin module), so the **`/admin/events`** module is visible only to staff/admin/dev (global `event.manage`); a purely club-scoped coordinator does not see it in the nav and must drive the scoped actions programmatically. A dedicated scoped-coordinator surface (reusing `assertEventManage`) is future work — the same posture as every other scoped role in the panel. | `lib/admin/server.mjs`, `app/admin/events`, `lib/events/authz.mjs` | ✅ Resolved (Session 13, DL-096) — a **STANDALONE `/coordinator`** surface (own scope-aware `loadCoordinatorContext`, NOT under the global `/admin` gate) now lets a club-scoped coordinator SEE and run their unit's **events** (settings/rounds/registrations/scores/attendance/closure-submit + CSV), **members** (roster + non-destructive CSV import), and **contribution** (the M6 club slice). A NEW inverse-of-the-resolver scoped-grant discovery (`lib/rbac/grants.mjs#listManageableLineages`, reusing `resolveEffectivePermissions` for exact parity) drives it; every action re-authorizes via the existing `assertEventManage` / `assertActorPermission` seams (organizer tagging / closure REVIEW / entities stay central `requireGlobal`). No new permission/table/migration. `tests/coordinator.db.test.mjs` 5/5 + `tests/coordinator.test.mjs` (13). |
| 44 | ⚪ | **M6 member-profile lists are not keyset-paginated (bounded per member, not per query).** The profile's event-involvement list (registrations ∪ scores ∪ attendance) and the contribution rollups are batched but return the member's FULL set — there is no `take`/cursor. This is bounded in practice (a member joins a limited number of events per year), and the durable rows already carry the cursor columns (`registeredAt`+`id`) if it ever needs paging. The `/admin/contribution` "participants reached" is a distinct COUNT (not a roster), so it is O(1)-shaped regardless of club size. A residual ~2 cheap single-table indexed re-reads (user/registrations/roles) remain across the two profile aggregators after the review's `getMemberProfileView` fix deduped the heavy achievements hydrate + year (DL-093). | `lib/member/profile.mjs`, `lib/member/contribution.mjs` | Accepted (Session 11 / M6, DL-090/091/093) |
| 45 | ⚪ | **Bulk mail sends synchronously inside the HTTP request (no job queue).** `sendBulk` paces batches with 60s inter-batch sleeps IN the request, so a large multi-batch send can exceed the request timeout and the single accounting audit row (written after the loop) is then lost. Mail is **operator-gated and off by default** (needs `npm install nodemailer` + `MAIL_*` — #40), so this is inert until configured. The correct fix (a background job/queue) is out of scope for a hardening session. | `lib/mail/service.mjs` | Accepted (Session 12, DL-095 / CONSOLIDATION_BUGLOG A1) — queue is future work |
| 46 | ⚪ | **Member "events participated" / touchpoints count includes waitlisted-only registrations.** `getMemberContribution` counts non-cancelled registrations as "participated"; a member who was only ever waitlisted (never confirmed / no score / no attendance) is included. Each row carries its `status`, so the UI distinguishes waitlisted; changing M6's aggregation semantics (DL-091) is a design change, not a hardening fix. | `lib/member/contribution.mjs` | Accepted (Session 12, DL-095 / CONSOLIDATION_BUGLOG A2) — semantic choice |
| 47 | ⚪ | **Two admin nav items can show for a permission the page's read then rejects (empty page).** `Users & Roles` lists `permission.override` in its nav `anyOf` (the page's users list needs `user.read`), and `Mail` lists `mail.manage` (the sender-list read needs `mail.send`). Only reachable via a NON-standard custom grant/override — no seeded role grants one without the other — and every such page still **fails closed** (renders a partial/empty view, never leaks data). Cosmetic dead-end. | `lib/admin/nav.mjs`, `app/admin/{users,mail}` | Accepted (Session 12, DL-095 / CONSOLIDATION_BUGLOG A4) |
| 48 | 🟠 | **Admin Event-Playground settings form could silently clear a coordinator's registrant-role restriction.** The form submitted blank and OVERWROTE `event_settings`; once the new `allowed_registrant_roles` (DL-097) was added, a save/"Go live now" merely to change capacity would reset the allow-list to "open to all" — an unintended authorization downgrade. | `app/admin/events/EventsClient.jsx`, `app/admin/events/page.jsx` | ✅ Resolved (Session 14) — the page now PRELOADS each event's stored settings (`getEventSettings`) and the form SEEDS capacity/window/roles from them (matching the coordinator client), so a save preserves the operator's values. Found by the Session-14 adversarial review. |
| 50 | ⚪ | **Inline edit-on-page refuses when an admin draft is already open (`409 DRAFT_OPEN`) — by design.** To avoid publishing an editor's unreviewed work-in-progress, `editAndPublish` (the inline "Save & publish" seam) will not act on an item that already has an open unpublished draft; the operator publishes/discards that draft in `/admin/content/[id]` first. The `DRAFT_OPEN` refusal now has live coverage (`tests/inline.db.test.mjs`, added Session 16). | `lib/cms/content.mjs`, `app/components/InlineEditor.jsx` | Accepted (Session 15, DL-103) — safe-by-design refusal; live test added Session 16 |
| 51 | ⚪ | **Live quizzes need `ioredis` installed only for the MULTI-INSTANCE path.** `lib/realtime/redis.mjs` keeps Redis LAZY + INJECTABLE (DL-107, the nodemailer pattern #40): with no `REDIS_URL` the live quiz/leaderboard runs fine SINGLE-INSTANCE (in-process SSE broadcaster + Postgres-authoritative leaderboard). To fan out SSE across multiple PM2 instances, start the `redis` service in `docker-compose.prod.yml`, set `REDIS_URL`, and `npm install ioredis`. Until then the Redis pub/sub bridge is inert (single-process delivery only). `ioredis` is intentionally NOT in `package.json`. | `lib/realtime/redis.mjs`, `docker-compose.prod.yml` | Operator step (Session 16, DL-107) |
| 52 | ⚪ | **Real-time state is per-process (in-memory), like the rate limiter #34.** The SSE broadcaster (`lib/realtime/broadcast.mjs`) fans out within ONE Node process; a naive PM2 **cluster** would fragment it (a publish in instance A wouldn't reach a subscriber on instance B) unless Redis pub/sub is enabled (#51) or the proxy uses sticky sessions. The committed topology is single-instance PM2 (systemRequirements §8), for which this is correct. A shared reveal/leaderboard broadcast also carries no per-viewer "you" highlight until the next per-connection snapshot/reconnect (the client derives its own reveal result locally). | `lib/realtime/broadcast.mjs` | Accepted (Session 16, DL-107; systemRequirements §8/§10) |
| 49 | ⚪ | **Working tree held unrelated pre-existing uncommitted edits at Session-14 start.** Files such as `app/components/{Header,Footer}.jsx`, `app/components/OrgUnitTabs.jsx`, `app/org/[type]/page.jsx`, `lib/org/{data/*,import,people}.mjs`, `lib/admin/reads.mjs`, `package.json`, `scripts/*`, and new/untracked `academic_council.png` / `public/Academic.png` / `scripts/seed-flagship.mjs` were already modified before this session and are now intermixed with the Session-14 diff. | working tree | Open — operator to review/commit the pre-existing work separately from the Session-14 quick-wins if a clean history is wanted |

> **Session 12 consolidation — full-site bug audit:** a per-feature × per-role audit surfaced 21 confirmed defects; **11 were fixed** (see `docs/CONSOLIDATION_BUGLOG.md` B1–B11 + the CHANGELOG) and the rest are the accepted items #45–#47 above plus minor/edge notes in the bug log. The 11 fixes are additive (the diff-review workflow found 0 regressions) and covered by new static + live regression assertions.

## Notes
- **Database pivot context:** several V1 issues are resolved by the move to
  PostgreSQL/Prisma rather than patched in place — #2 (events API auth) and #8
  (allowlist) → **✅ resolved in Session 2** (auth/RBAC); #5/#9/#16 (base64 images, validation,
  no edit/delete) and #3 (past-events contract) → **✅ resolved in Session 6** (events
  rebuilt on Postgres via the CMS service); #6/#7 (Mongoose connection) are now moot in the
  request path (Mongoose retired from `app/api/events`; `lib/db.js`/`models/Event.js`
  remain only for the read-only backup script `scripts/export-events.mjs`).
- #4 (pdf.js) and #11/#12 (brand blue, fonts) are UI/library issues addressed when
  the relevant pages are reworked (Sessions 5–9 / 10).
- See [docs/MILESTONE_PLAN.md](docs/MILESTONE_PLAN.md) for session mapping.
